#!/usr/bin/python

# A module for processing text description for an SVG callflow.

def textPlacer (session, svgFile, textDescription, tx, ty):
    "Given a text description, chop up the text in pieces and \
    place the text in rows such that the output looks pretty"

    return
